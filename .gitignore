desktop.ini
node_modules/

# Nunca commitear secretos reales si alguien crea un .env local para pruebas
.env
.env.local
.env*.local

# Archivos temporales/bloqueo de Microsoft Word
~$*
*.tmp
~WRL*.tmp

# Vídeos exportados de marketing (se regeneran con los scripts)
marketing/*.mp4

# Archivos locales que no pertenecen al repo del sitio
.claude/
*.docx
